type: botforge/fragment
steps:
- on: host
  name: emit secret
  id: emit
  run: |
    echo "token=steel-toe-secret" >> "$BF_OUT"
  outputs:
  - name: token
    type: secret
    required: true

outputs:
- name: token
  type: string
  from-step: emit
  from-output: token
  required: true
