type: botforge/fragment
steps:
- on: host
  name: emit secret
  id: emit
  run: |
    echo "token=steel-toe-secret" >> "$BF_OUT"
  outputs:
    token:
      type: secret
      required: true

outputs:
  token:
    type: string
    from_step: emit
    from_output: token
    required: true
